{
  "name": "@clocked/heyanon",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@clocked/core": "workspace:*",
    "@clocked/db": "workspace:*",
    "zod": "^3.24.3"
  }
}
